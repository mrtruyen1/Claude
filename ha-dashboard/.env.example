# Base URL of the Home Assistant instance this dashboard controls.
HA_URL=http://192.168.31.111:8123

# Long-lived access token, created in HA under Profile -> Security ->
# "Long-lived access tokens". Server-side only, never sent to the browser.
HA_TOKEN=

# Port the Next.js server listens on.
PORT=3000
